# Allow direct access to receipt files for preview
# Disable directory listing for security

Options -Indexes

# Allow image and PDF files to be accessed directly
<FilesMatch "\.(jpg|jpeg|png|pdf)$">
    Order Allow,Deny
    Allow from all
</FilesMatch>
